#include <cstdio>
#include <cstring>

int main (void)
{
	int t;
	scanf ("%d", &t);
	
	while (t--)
	{
		int n;
		scanf ("%d", &n);
		int fact[158];
		for (int i = 0; i < 158; i++)
		  fact[i] = 0;
		int count = 0;
		
	}
}